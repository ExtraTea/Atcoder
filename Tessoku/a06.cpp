#include <bits/stdc++.h>
using namespace std;
int main(void){
    int n,q;
    cin >>n >>q;
    vector<int> a(n), l(q), r(q);
    for(int i=0; i<n; i++) cin >>a.at(i);
    for(int i=0; i<q; i++) cin >>l.at(i) >>r.at(i);

    

}